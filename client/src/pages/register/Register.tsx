import { Typography } from "@/components";
import { useDocumentTitle } from "@/hooks";

const Register = () => {
  useDocumentTitle("Register");

  return <Typography variant={"heading-1"}>Register</Typography>;
};

export default Register;
