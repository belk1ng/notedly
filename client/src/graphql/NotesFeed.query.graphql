query NotesFeed($cursor: String!) {
    notesFeed(cursor: $cursor) {
        notes {
            id
            createdAt
            content
            author {
                username
            }
        }
    }
}