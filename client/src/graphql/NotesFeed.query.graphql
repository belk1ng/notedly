query NotesFeed($cursor: String!, $limit: Int) {
    notesFeed(cursor: $cursor, limit: $limit) {
        notes {
            id
            createdAt
            content
            author {
                username
            }
        }
        hasNextPage
        cursor
    }
}